# admin_users (additive schema note)

Documented in [WEB_ADMIN_ARCHITECTURE.md](WEB_ADMIN_ARCHITECTURE.md).

Collection: `admin_users/{uid}` — web admin roles only. Mobile continues to use `users.role` (player/scorer/organizer/…).
