/// Persisted sub-classification for [WicketType.caught] dismissals.
class DismissalSubType {
  DismissalSubType._();

  /// Keeper catch — stored as wicketType `caught` + this sub-type.
  static const caughtBehind = 'caught_behind';
}
